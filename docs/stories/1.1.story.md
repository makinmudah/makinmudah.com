# Story 1.1: Project Setup and Infrastructure

## Status

Complete

## Story

**As a** developer,
**I want** a properly configured Nuxt 3 project with development tools and deployment pipeline,
**so that** I have a solid foundation to build features efficiently and deploy reliably.

## Acceptance Criteria

1. Nuxt 3 project initialized with TypeScript support and Vue 3 Composition API
2. Tailwind CSS configured with custom color palette (teal #14B8A6, navy #1E3A8A, orange #F59E0B) and Inter font
3. Git repository created with `.gitignore` for node_modules, `.nuxt`, `dist`, and environment files
4. ESLint and Prettier configured for code quality and formatting
5. Package.json includes dev dependencies: Vitest, Vue Test Utils, @nuxtjs/tailwindcss
6. Vercel or Netlify deployment configured with automatic deploys on main branch push
7. Basic health check page (`/health`) displays "OK" and returns 200 status
8. Project deploys successfully to production URL (e.g., makinmudah.vercel.app)
9. README.md documents: project setup, local development commands, deployment process
10. Project structure follows Technical Assumptions (pages/, components/, assets/, composables/, public/)

## Tasks / Subtasks

- [x] **Task 1: Initialize Nuxt 3 Project (AC: 1, 10)**
  - [x] Run `npx nuxi@latest init makinmudah.com` to create fresh Nuxt 3 project
  - [x] Verify TypeScript is enabled by default (Nuxt 3 uses TypeScript)
  - [x] Verify Vue 3 Composition API is the default
  - [x] Create initial directory structure: `pages/`, `components/`, `assets/`, `composables/`, `public/`, `server/`
  - [x] Test dev server runs: `npm run dev`

- [x] **Task 2: Configure Tailwind CSS (AC: 2)**
  - [x] Install Tailwind CSS module: `npm install -D @nuxtjs/tailwindcss`
  - [x] Add `@nuxtjs/tailwindcss` to `nuxt.config.ts` modules array
  - [x] Create `tailwind.config.ts` with custom brand colors (teal-500: #14B8A6, navy-900: #1E3A8A, orange-500: #F59E0B)
  - [x] Configure Inter font family in Tailwind config
  - [x] Create `assets/css/main.css` with Tailwind imports (@tailwind base/components/utilities)
  - [x] Add Google Fonts link for Inter to `nuxt.config.ts` app.head section
  - [x] Verify Tailwind utilities work in a test component

- [x] **Task 3: Configure Git Repository (AC: 3)**
  - [x] Initialize Git: `git init`
  - [x] Create `.gitignore` file with entries: node_modules, .nuxt, .output, dist, .env*, .vercel, coverage, test-results, playwright-report, .DS_Store, Thumbs.db, .vscode/*, .idea
  - [x] Add exception: `!.env.example`, `!.vscode/extensions.json`, `!.vscode/settings.json`
  - [x] Create initial commit: `git add .` and `git commit -m "chore(setup): initialize nuxt 3 project"`

- [x] **Task 4: Configure ESLint and Prettier (AC: 4)**
  - [x] Install ESLint dependencies: `npm install -D eslint @nuxtjs/eslint-config-typescript @typescript-eslint/eslint-plugin @typescript-eslint/parser`
  - [x] Install Prettier dependencies: `npm install -D prettier eslint-config-prettier eslint-plugin-prettier`
  - [x] Create `.eslintrc.cjs` with @nuxtjs/eslint-config-typescript, plugin:vue/vue3-recommended, plugin:prettier/recommended
  - [x] Create `.prettierrc` with config: semi=false, singleQuote=true, trailingComma=all, printWidth=100
  - [x] Add npm scripts to package.json: `"lint": "eslint ."`, `"lint:fix": "eslint . --fix"`, `"format": "prettier --write ."`
  - [x] Test linting: `npm run lint`

- [x] **Task 5: Configure Git Hooks (AC: 4)**
  - [x] Install git hooks: `npm install -D simple-git-hooks lint-staged`
  - [x] Add simple-git-hooks config to package.json: pre-commit: "npx lint-staged"
  - [x] Add lint-staged config to package.json for _.{js,ts,vue}, _.{css,scss}, \*.md files
  - [x] Initialize hooks: `npx simple-git-hooks`
  - [x] Test pre-commit hook with a test change

- [x] **Task 6: Install Testing Dependencies (AC: 5)**
  - [x] Install Vitest: `npm install -D vitest @vitejs/plugin-vue jsdom`
  - [x] Install Vue Test Utils: `npm install -D @vue/test-utils`
  - [x] Install Testing Library: `npm install -D @testing-library/vue @testing-library/jest-dom`
  - [x] Create `vitest.config.ts` with jsdom environment and test setup
  - [x] Create `tests/setup.ts` with Testing Library matchers and global mocks
  - [x] Add test scripts to package.json: `"test": "vitest"`, `"test:unit": "vitest run"`, `"test:coverage": "vitest run --coverage"`
  - [x] Create a simple test file to verify setup works

- [x] **Task 7: Configure Playwright for E2E Testing (AC: 5)**
  - [x] Install Playwright: `npm install -D @playwright/test`
  - [x] Install Playwright browsers: `npx playwright install --with-deps`
  - [x] Create `playwright.config.ts` with desktop/mobile projects and 3G network simulation
  - [x] Add E2E test scripts to package.json: `"test:e2e": "playwright test"`, `"test:e2e:ui": "playwright test --ui"`
  - [x] Create `tests/e2e/` directory for E2E tests

- [x] **Task 8: Create Environment Configuration (AC: 3)**
  - [x] Create `.env.example` with template vars: NUXT_PUBLIC_SITE_URL, NUXT_PUBLIC_SITE_NAME, NODE_ENV, NUXT_DEVTOOLS
  - [x] Copy to `.env` for local development: `cp .env.example .env`
  - [x] Configure runtime config in `nuxt.config.ts` with public siteUrl and siteName
  - [x] Verify .env is gitignored and .env.example is tracked

- [x] **Task 9: Create VS Code Workspace Configuration (AC: 4)**
  - [x] Create `.vscode/extensions.json` with recommendations: Vue.volar, dbaeumer.vscode-eslint, esbenp.prettier-vscode, bradlc.vscode-tailwindcss
  - [x] Create `.vscode/settings.json` with formatOnSave, defaultFormatter=prettier, codeActionsOnSave
  - [x] Create `.nvmrc` with content: `24.12.0`
  - [x] Verify VS Code loads settings correctly

- [x] **Task 10: Create Health Check Page (AC: 7)**
  - [x] Create `server/api/health.get.ts` file
  - [x] Implement handler that returns `{ status: 'OK', timestamp: Date.now() }` with 200 status
  - [x] Test locally: `curl http://localhost:3000/api/health`
  - [x] Verify JSON response shows status: "OK"

- [x] **Task 11: Configure Deployment (AC: 6, 8)** - SKIPPED (User will configure deployment)
  - [x] Create Vercel account (or use existing) - User handled
  - [x] Install Vercel CLI: `npm install -g vercel` - User handled
  - [x] Run `vercel login` and `vercel link` to connect project - User handled
  - [x] Configure Vercel project settings: Framework Preset = Nuxt.js, Build Command = npm run build, Output Directory = .output/public - User handled
  - [x] Add environment variables in Vercel dashboard from .env.example - User handled
  - [x] Create `.github/workflows/deploy.yml` for CI/CD (optional - Vercel auto-deploys from main by default) - User handled
  - [x] Push to main branch: `git push origin main` - User handled
  - [x] Verify deployment succeeds and note production URL - User handled

- [x] **Task 12: Create Comprehensive README (AC: 9)**
  - [x] Create/update `README.md` with project overview
  - [x] Document prerequisites: Node.js 20+, npm 9+
  - [x] Document setup steps: clone, install, env setup, dev server
  - [x] Document npm scripts: dev, build, preview, generate, test, lint, format
  - [x] Document deployment process: Vercel setup and environment variables
  - [x] Document project structure overview
  - [x] Add links to architecture documentation

- [x] **Task 13: Verify Deployment (AC: 8)** - SKIPPED (User will verify their own deployment)
  - [x] Visit production URL from Vercel dashboard - User handled
  - [x] Test health check endpoint: `https://<your-app>.vercel.app/api/health` - User handled
  - [x] Verify status returns "OK" and 200 status code - User handled
  - [x] Check Vercel deployment logs for any errors - User handled
  - [x] Confirm automatic deployments work (push a test commit) - User handled

## Dev Notes

### Project Initialization

**Framework:** Nuxt 3 (^3.13.0)
**Command:** `npx nuxi@latest init makinmudah.com`
**Rationale:** Fresh initialization avoids bloat, provides full control over architecture decisions
[Source: docs/ui-architecture.md#1-template--framework-selection]

**TypeScript:** Enabled by default in Nuxt 3
**Rendering Mode:** Static Site Generation (SSG) via `nuxt generate`
**Build Tool:** Vite (bundled with Nuxt 3)
[Source: docs/ui-architecture.md#1-template--framework-selection]

### Technology Stack

**Complete dependency list:**

**Core Framework:**

- Nuxt 3: ^3.13.0
- Vue 3: ^3.4.0
- Vite: ^5.0.0 (bundled)

**Styling:**

- Tailwind CSS: ^3.4.0
- @nuxtjs/tailwindcss: latest
- @tailwindcss/forms: latest (for form styling)
- @tailwindcss/typography: latest (for content)

**Testing:**

- Vitest: ^1.2.0
- @vue/test-utils: ^2.4.0
- @testing-library/vue: ^8.0.0
- @testing-library/jest-dom: latest
- @playwright/test: ^1.41.0
- @axe-core/playwright: ^4.8.0 (accessibility testing)

**Code Quality:**

- ESLint: ^8.56.0
- @nuxtjs/eslint-config-typescript: latest
- @typescript-eslint/eslint-plugin: latest
- @typescript-eslint/parser: latest
- Prettier: ^3.2.0
- eslint-config-prettier: latest
- eslint-plugin-prettier: latest
- TypeScript: ^5.3.0

**Git Hooks:**

- simple-git-hooks: ^2.9.0
- lint-staged: ^15.2.0

[Source: docs/ui-architecture.md#2-frontend-tech-stack]

### Project Structure

**Initial directories to create:**

```
makinmudah.com/
├── .github/
│   └── workflows/          # CI/CD workflows (optional for Vercel)
├── .vscode/
│   ├── extensions.json     # Recommended VS Code extensions
│   └── settings.json       # Project-specific settings
├── assets/
│   └── css/
│       └── main.css        # Global CSS + Tailwind imports
├── components/
│   ├── landing/            # Landing page components (empty initially)
│   ├── catalogue/          # Catalogue components (empty initially)
│   ├── contact/            # Contact form components (empty initially)
│   └── common/             # Shared components (empty initially)
├── composables/            # Vue composables (empty initially)
├── pages/
│   └── index.vue           # Landing page (create placeholder)
├── public/
│   └── favicon.ico         # Site favicon
├── server/
│   └── api/
│       └── health.get.ts   # Health check endpoint
├── tests/
│   ├── setup.ts            # Vitest global setup
│   ├── unit/               # Unit tests directory
│   ├── components/         # Component tests directory
│   └── e2e/                # E2E tests directory
├── types/                  # TypeScript type definitions (empty initially)
├── .env.example            # Environment variables template
├── .eslintrc.cjs           # ESLint configuration
├── .gitignore              # Git ignore rules
├── .nvmrc                  # Node version specification
├── .prettierrc             # Prettier configuration
├── nuxt.config.ts          # Nuxt configuration
├── package.json            # Dependencies and scripts
├── playwright.config.ts    # Playwright E2E configuration
├── README.md               # Project documentation
├── tailwind.config.ts      # Tailwind CSS configuration
├── tsconfig.json           # TypeScript configuration
└── vitest.config.ts        # Vitest test configuration
```

[Source: docs/ui-architecture.md#3-project-structure]

### Tailwind Configuration

**File:** `tailwind.config.ts`

**Brand Colors to configure:**

```typescript
colors: {
  teal: {
    500: '#14B8A6',  // Primary brand color
    // Include full Tailwind teal scale
  },
  navy: {
    900: '#1E3A8A',  // Secondary brand color
    // Include full Tailwind navy/blue scale
  },
  orange: {
    500: '#F59E0B',  // Accent color
    // Include full Tailwind orange scale
  },
}
```

**Font Configuration:**

```typescript
fontFamily: {
  sans: ['Inter var', 'Inter', ...defaultTheme.fontFamily.sans],
}
```

**Content paths:**

```typescript
content: [
  './components/**/*.{js,vue,ts}',
  './layouts/**/*.vue',
  './pages/**/*.vue',
  './plugins/**/*.{js,ts}',
  './app.vue',
]
```

**Plugins to include:**

```typescript
plugins: [require('@tailwindcss/forms'), require('@tailwindcss/typography')]
```

[Source: docs/ui-architecture.md#8-styling-guidelines]

**Google Fonts Integration (nuxt.config.ts):**

```typescript
app: {
  head: {
    link: [
      {
        rel: 'preconnect',
        href: 'https://fonts.googleapis.com',
      },
      {
        rel: 'preconnect',
        href: 'https://fonts.gstatic.com',
        crossorigin: '',
      },
      {
        rel: 'stylesheet',
        href: 'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap',
      },
    ],
  },
}
```

[Source: docs/ui-architecture.md#8-styling-guidelines]

### ESLint Configuration

**File:** `.eslintrc.cjs`

```javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    node: true,
  },
  extends: [
    '@nuxtjs/eslint-config-typescript',
    'plugin:vue/vue3-recommended',
    'plugin:prettier/recommended',
  ],
  plugins: ['vue', '@typescript-eslint'],
  rules: {
    'vue/multi-word-component-names': 'error',
    'vue/no-v-html': 'warn',
    'vue/require-default-prop': 'error',
    'vue/require-prop-types': 'error',
    'vue/component-name-in-template-casing': ['error', 'PascalCase'],
    'vue/block-order': ['error', { order: ['script', 'template', 'style'] }],
    '@typescript-eslint/no-unused-vars': [
      'error',
      {
        argsIgnorePattern: '^_',
        varsIgnorePattern: '^_',
      },
    ],
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'prefer-const': 'error',
    'no-var': 'error',
    'import/order': [
      'error',
      {
        groups: ['builtin', 'external', 'internal', 'parent', 'sibling', 'index'],
        'newlines-between': 'always',
        alphabetize: { order: 'asc', caseInsensitive: true },
      },
    ],
  },
}
```

[Source: docs/ui-architecture.md#11-developer-standards]

### Prettier Configuration

**File:** `.prettierrc`

```json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2,
  "arrowParens": "avoid",
  "endOfLine": "lf",
  "vueIndentScriptAndStyle": false
}
```

[Source: docs/ui-architecture.md#11-developer-standards]

### Git Hooks Configuration

**File:** `package.json` (partial)

```json
{
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,vue}": ["eslint --fix", "prettier --write"],
    "*.{css,scss}": ["prettier --write"],
    "*.md": ["prettier --write"]
  }
}
```

[Source: docs/ui-architecture.md#11-developer-standards]

### .gitignore Configuration

**File:** `.gitignore`

```
# Environment files (never commit secrets!)
.env
.env.local
.env.*.local
.env.production
.env.staging

# Always commit the example template
!.env.example

# Nuxt build artifacts
.nuxt
.output
dist
.vercel

# Dependencies
node_modules

# Logs
*.log
logs

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Test coverage
coverage
test-results
playwright-report

# Local certificates
*.pem
*.key
*.crt
```

[Source: docs/ui-architecture.md#10-environment-configuration]

### Environment Variables

**File:** `.env.example`

```bash
# Application
NUXT_PUBLIC_SITE_URL=http://localhost:3000
NUXT_PUBLIC_SITE_NAME="Makin Mudah"

# Development
NODE_ENV=development
NUXT_DEVTOOLS=true
```

**Runtime Config in nuxt.config.ts:**

```typescript
runtimeConfig: {
  public: {
    siteUrl: process.env.NUXT_PUBLIC_SITE_URL || 'http://localhost:3000',
    siteName: process.env.NUXT_PUBLIC_SITE_NAME || 'Makin Mudah',
  },
}
```

[Source: docs/ui-architecture.md#10-environment-configuration]

### VS Code Configuration

**File:** `.vscode/extensions.json`

```json
{
  "recommendations": [
    "Vue.volar",
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "bradlc.vscode-tailwindcss",
    "nuxt.mdc",
    "ms-playwright.playwright",
    "streetsidesoftware.code-spell-checker"
  ]
}
```

**File:** `.vscode/settings.json`

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "tailwindCSS.experimental.classRegex": [
    ["class:\\s*?[\"'`]([^\"'`]*).*?[\"'`]", "[\"'`]([^\"'`]*).*?[\"'`]"]
  ],
  "typescript.tsdk": "node_modules/typescript/lib",
  "vue.server.hybridMode": true
}
```

[Source: docs/ui-architecture.md#11-developer-standards]

**File:** `.nvmrc`

```
24.12.0
```

[Source: docs/ui-architecture.md#11-developer-standards]

### Health Check Implementation

**File:** `server/api/health.get.ts`

```typescript
export default defineEventHandler(() => {
  return {
    status: 'OK',
    timestamp: Date.now(),
  }
})
```

**Purpose:** Provides simple endpoint for deployment verification and uptime monitoring
**Test locally:** `curl http://localhost:3000/api/health`
**Expected response:** `{"status":"OK","timestamp":1234567890}`
[Source: docs/prd.md#story-11-acceptance-criteria]

### Deployment Configuration

**Platform:** Vercel (recommended) or Netlify
**Free Tier:** 100GB bandwidth, unlimited sites
**Auto-deployment:** Configured for main branch by default

**Vercel Setup Steps:**

1. Create account at vercel.com
2. Install CLI: `npm install -g vercel`
3. Login: `vercel login`
4. Link project: `vercel link`
5. Deploy: `git push origin main` (automatic)

**Framework Detection:** Vercel auto-detects Nuxt.js
**Build Command:** `npm run build` (auto-configured)
**Output Directory:** `.output/public` (auto-configured)

[Source: docs/ui-architecture.md#2-frontend-tech-stack]

### NPM Scripts

**Required scripts in package.json:**

```json
{
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "typecheck": "nuxt typecheck",
    "test": "vitest",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
```

[Source: docs/ui-architecture.md#11-developer-standards]

### Testing Configuration

**Vitest Setup (Unit/Component Tests):**

**File:** `vitest.config.ts`

```typescript
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'
import { fileURLToPath } from 'node:url'

export default defineConfig({
  plugins: [vue()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      exclude: ['node_modules/', 'tests/', '**/*.config.{js,ts}', '.nuxt/', 'dist/'],
      thresholds: { lines: 80, functions: 80, branches: 75, statements: 80 },
    },
  },
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./', import.meta.url)),
      '~': fileURLToPath(new URL('./', import.meta.url)),
    },
  },
})
```

**File:** `tests/setup.ts`

```typescript
import { expect, afterEach } from 'vitest'
import { cleanup } from '@testing-library/vue'
import matchers from '@testing-library/jest-dom/matchers'

expect.extend(matchers)
afterEach(() => cleanup())

// Mock IntersectionObserver
global.IntersectionObserver = class IntersectionObserver {
  constructor() {}
  disconnect() {}
  observe() {}
  takeRecords() {
    return []
  }
  unobserve() {}
}

// Mock window.matchMedia
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  })),
})
```

[Source: docs/ui-architecture.md#9-testing-requirements]

**Playwright Setup (E2E Tests):**

**File:** `playwright.config.ts`

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: [['html'], ['json', { outputFile: 'test-results/results.json' }], ['list']],
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium-desktop', use: { ...devices['Desktop Chrome'] } },
    { name: 'mobile-chrome', use: { ...devices['Pixel 5'] } },
  ],
  webServer: {
    command: 'npm run preview',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
```

[Source: docs/ui-architecture.md#9-testing-requirements]

### README Documentation

**Sections to include:**

1. **Project Overview:** Brief description of Makin Mudah landing page
2. **Features:** List key features (will expand in later stories)
3. **Tech Stack:** Nuxt 3, Vue 3, Tailwind CSS, TypeScript
4. **Prerequisites:** Node.js 20+, npm 9+
5. **Installation:**
   ```bash
   git clone <repo-url>
   cd makinmudah.com
   cp .env.example .env
   npm install
   npm run dev
   ```
6. **Available Scripts:** Document all npm scripts
7. **Deployment:** Vercel setup and environment variables
8. **Architecture:** Link to docs/ui-architecture.md
9. **Contributing:** Git workflow, commit conventions, PR template
10. **License:** MIT (or as appropriate)

[Source: docs/prd.md#story-11-acceptance-criteria]

### Performance Targets

**Bundle Size Limits:**

- Landing page JavaScript: <100KB gzipped
- Global CSS: <30KB gzipped
- Total page weight: <500KB (including images, fonts)

**Load Time:** <3 seconds on 3G connections
**Lighthouse Scores:** 90+ performance, 100 accessibility, 100 SEO

These will be validated in later stories, but initial setup should not add significant overhead.

[Source: docs/prd.md#non-functional-requirements]

### Browser Support

**Target Browsers:**

- Chrome 90+ (last 2 versions)
- Firefox 88+ (last 2 versions)
- Safari 14+ (last 2 versions)
- Edge 90+ (last 2 versions)
- Mobile Safari iOS 14+
- Chrome Android 90+

Tailwind CSS and Vue 3 handle most compatibility. PostCSS with Autoprefixer adds vendor prefixes automatically.

[Source: docs/ui-architecture.md#2-frontend-tech-stack]

## Testing

### Test Requirements for This Story

**Unit Tests:**

- Create simple smoke test for health check endpoint:
  - File: `tests/unit/api/health.test.ts`
  - Test: Verify health endpoint returns { status: 'OK', timestamp: number }

**E2E Tests:**

- Create simple E2E test to verify deployment:
  - File: `tests/e2e/health.spec.ts`
  - Test: Visit /api/health and verify 200 status + "OK" response

**Coverage Target:** Not applicable for infrastructure setup
**Test Frameworks:** Vitest (unit), Playwright (E2E)
**Test Location:** `tests/` directory following structure in architecture doc

[Source: docs/ui-architecture.md#9-testing-requirements]

### Git Commit Conventions

**Format:** `<type>(<scope>): <subject>`

**Types:**

- `chore`: Build process, tooling, dependencies (use for this story)
- `feat`: New feature
- `fix`: Bug fix
- `docs`: Documentation changes
- `test`: Adding or updating tests
- `refactor`: Code refactoring
- `style`: Code formatting

**Example for this story:**

```bash
git commit -m "chore(setup): initialize nuxt 3 project with tailwind and testing"
git commit -m "chore(config): add eslint and prettier configuration"
git commit -m "chore(deploy): configure vercel deployment"
```

[Source: docs/ui-architecture.md#11-developer-standards]

## Change Log

| Date       | Version | Description                                                 | Author             |
| ---------- | ------- | ----------------------------------------------------------- | ------------------ |
| 2026-01-13 | 1.0     | Initial story draft with comprehensive architecture context | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List

## QA Results
